---
#==============================================================#
# File      :   citus.yml
# Desc      :   pigsty citus demo with 1 coordinator and 4 data nodes
# Ctime     :   2020-05-22
# Mtime     :   2023-02-11
# Docs      :   https://github.com/Vonng/pigsty/wiki/Configuration
# Author    :   Ruohang Feng (rh@vonng.com)
# License   :   AGPLv3
#==============================================================#


all:
  children:

    # infra cluster for proxy, monitor, alert, etc..
    infra: { hosts: { 10.10.10.10: { infra_seq: 1 } } }

    # minio cluster, s3 compatible object storage
    # minio: { hosts: { 10.10.10.10: { minio_seq: 1 } }, vars: { minio_cluster: minio } }

    # etcd cluster for ha postgres
    etcd: { hosts: { 10.10.10.10: { etcd_seq: 1 } }, vars: { etcd_cluster: etcd } }

    # citus coordinator, pg_group = 0
    pg-citus0:
      hosts:
        10.10.10.10: { pg_seq: 1, pg_role: primary , pg_offline_query: true }
      vars: { pg_cluster: pg-citus0 , pg_group: 0 }

    # citus data node, pg_group = 1,2,3,4 (4 node instead of two p/s pair)
    pg-citus1:
      hosts: { 10.10.10.11: { pg_seq: 1, pg_role: primary } }
      vars: { pg_cluster: pg-citus1 , pg_group: 1 }
    pg-citus2:
      hosts: { 10.10.10.11: { pg_seq: 1, pg_role: primary } }
      vars: { pg_cluster: pg-citus2 , pg_group: 2 }
    pg-citus3:
      hosts: { 10.10.10.11: { pg_seq: 1, pg_role: primary } }
      vars: { pg_cluster: pg-citus3 , pg_group: 3 }
    pg-citus4:
      hosts: { 10.10.10.11: { pg_seq: 1, pg_role: primary } }
      vars: { pg_cluster: pg-citus4 , pg_group: 4 }

    # citus data node, pg_group = 1,2
    #pg-citus1:
    #  hosts:
    #    10.10.10.11: { pg_seq: 1, pg_role: primary }
    #    10.10.10.12: { pg_seq: 2, pg_role: replica }
    #  vars: { pg_cluster: pg-citus1 , pg_group: 1 }
    #pg-citus2:
    #  hosts:
    #    10.10.10.13: { pg_seq: 1, pg_role: primary }
    #    10.10.10.14: { pg_seq: 2, pg_role: replica }
    #  vars: { pg_cluster: pg-citus2 , pg_group: 2 }


  vars: # global variables
    # global citus config for all citus clusters
    pg_mode: citus                    # pgsql cluster mode: pgsql,citus,gpsql
    pg_conf: citus.yml                # citus configuration
    pg_shard: pg-citus                # citus shard name
    patroni_citus_db: meta            # citus distributed database name
    pg_users: [ { name: citus ,password: citus ,pgbouncer: true ,roles: [ dbrole_admin ] } ]
    pg_databases: [ { name: meta ,owner: citus , extensions: [ { name: citus }, { name: postgis }, { name: timescaledb } ] } ]
    pg_libs: 'citus, timescaledb, pg_stat_statements, auto_explain'
    pg_hba_rules:
      - title: Allow dbsu db/meta ssl access from local/shard
        role: common
        rules:
          - hostssl  meta  postgres  127.0.0.1/32    trust
          - hostssl  meta  postgres  10.10.10.10/32  trust
          - hostssl  meta  postgres  10.10.10.11/32  trust
          - hostssl  meta  postgres  10.10.10.12/32  trust
          - hostssl  meta  postgres  10.10.10.13/32  trust
          - hostssl  meta  postgres  10.10.10.14/32  trust

    version: v2.0.0-b6                # pigsty version string
    admin_ip: 10.10.10.10             # admin node ip address
    region: default                   # upstream mirror region: default|china|europe
    node_etc_hosts:                   # write static dns for better reliability
      - 10.10.10.11 node-1 pg-node1-1
      - 10.10.10.12 node-2 pg-node2-1
      - 10.10.10.13 node-3 pg-node3-1
      - 10.10.10.14 node-4 pg-node4-1

...