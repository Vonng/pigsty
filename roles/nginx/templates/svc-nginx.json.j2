{
  "service": {
    "name": "nginx",
    "tags": ["meta"],
    "meta": {"type": "nginx"},
    "port": 80,
    "check": {
      "tcp": "localhost:80",
      "interval": "{{ consul_check_interval }}",
      "timeout": "{{ consul_check_timeout  }}"
    }
  }
}