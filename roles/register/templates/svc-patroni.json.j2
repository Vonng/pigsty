{
  "service": {
    "name": "patroni",
    "port": {{ patroni_port }},
    "tags": [
      "pgsql",
      "{{ pg_role }}",
      "{{ pg_cluster }}"
    ],
    "meta": {
      "type": "patroni",
      "role": "{{ pg_role }}",
      "seq": "{{ pg_seq }}",
      "instance": "{{ pg_cluster }}-{{ pg_seq }}",
      "service": "{{ pg_service }}",
      "cluster": "{{ pg_cluster }}",
      "version": "{{ pg_version }}"
    },
    "check": {
      "tcp": "127.0.0.1:{{ patroni_port }}",
      "interval": "15s",
      "timeout": "1s"
    }
  }
}