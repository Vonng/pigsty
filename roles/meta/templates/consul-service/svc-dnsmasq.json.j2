{
  "service": {
    "name": "dnsmasq",
    "tags": ["meta"],
    "meta": {"type": "dnsmasq"},
    "port": 53,
    "check": {
      "tcp": "localhost:53",
      "interval": "{{ dcs_check_interval }}",
      "timeout": "{{ dcs_check_timeout  }}"
    }
  }
}