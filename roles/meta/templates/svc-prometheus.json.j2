{
  "service": {
    "name": "prometheus",
    "tags": ["meta", "exporter"],
    "meta": {"type": "prometheus"},
    "port": 9090,
    "check": {
      "tcp": "localhost:9090",
      "interval": "{{ dcs_check_interval }}",
      "timeout": "{{ dcs_check_timeout  }}"
    }
  }
}