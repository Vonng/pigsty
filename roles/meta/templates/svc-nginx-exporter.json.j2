{
  "service": {
    "name": "nginx-exporter",
    "tags": ["meta", "exporter"],
    "meta": {"type": "exporter"},
    "port": 9113,
    "check": {
      "http": "http://localhost:9113/",
      "interval": "{{ dcs_check_interval }}",
      "timeout": "{{ dcs_check_timeout  }}"
    }
  }
}