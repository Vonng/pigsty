---
#------------------------------------------------------------------------------
# Remove Backup
#------------------------------------------------------------------------------
- name: remove postgres backup
  become: yes
  ignore_errors: yes
  when: rm_pgbkup|bool
  file: path={{ item }} state=absent
  with_items:
    - "{{ pg_fs_bkup }}/backup"

...