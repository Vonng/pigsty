MINIO_OPTS="--address :{{ minio_port }} --console-address :{{ minio_admin_port }}"
MINIO_VOLUMES="{{ minio_volumes }}"
MINIO_ROOT_USER="{{ minio_access_key }}"
MINIO_ROOT_PASSWORD="{{ minio_secret_key }}"
MINIO_PROMETHEUS_AUTH_TYPE=public
MINIO_PROMETHEUS_JOB_ID=minio
MINIO_PROMETHEUS_URL=http://{{ admin_ip|default('10.10.10.10') }}:9090

# ENVIRONMENT from minio_extra_vars
{{ minio_extra_vars }}