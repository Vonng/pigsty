{
  "service": {
    "name": "patroni",
    "port": {{ patroni_port }},
    "tags": [
      "pgsql",
      "exporter",
      "{{ pg_cluster }}"
    ],
    "meta": {
      "type": "patroni",
      "seq": "{{ pg_seq }}",
      "instance": "{{ pg_cluster }}-{{ pg_seq }}",
      "cluster": "{{ pg_cluster }}"
    },
    "check": {
      "tcp": "127.0.0.1:{{ patroni_port }}",
      "interval": "15s",
      "timeout": "1s"
    }
  }
}