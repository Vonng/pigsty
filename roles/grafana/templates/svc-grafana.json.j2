{
  "service": {
    "name": "grafana",
    "tags": ["meta", "exporter"],
    "meta": {"type": "grafana"},
    "port": 3000,
    "check": {
      "tcp": "localhost:3000",
      "interval": "{{ consul_check_interval }}",
      "timeout": "{{ consul_check_timeout  }}"
    }
  }
}