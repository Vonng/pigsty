{
  "service": {
    "name": "pg-exporter",
    "port": 9630,
    "tags": [
      "pg-exporter",
      "exporter"
    ],
    "meta": {
      "type": "exporter",
      "role": "{{ role }}",
      "seq": "{{ seq }}",
      "instance": "{{ cluster }}-{{seq}}",
      "service": "{{ cluster }}-{{ role }}",
      "cluster": "{{ cluster }}"
    },
    "check": {
      "http": "http://{{ inventory_hostname }}:9630/",
      "interval": "5s",
      "timeout": "1s"
    }
  }
}