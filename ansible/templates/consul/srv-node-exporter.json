{
  "service": {
    "name": "node-exporter",
    "port": 9100,
    "tags": [
      "node-exporter",
      "exporter"
    ],
    "meta": {
      "type": "exporter",
      "role": "{{ role }}",
      "seq": "{{ seq }}",
      "instance": "{{ cluster }}-{{seq}}",
      "service": "{{ cluster }}-{{ role }}",
      "cluster": "{{ cluster }}"
    },
    "check": {
      "http": "http://{{ inventory_hostname }}:9100/",
      "interval": "5s",
      "timeout": "1s"
    }
  }
}