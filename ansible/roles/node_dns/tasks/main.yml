---
- block:
    # redhat/centos: install chronyd
    - name: Add dns hosts
      tags: node_dns_etc_hosts
      script: pigsty_hosts.sh

    - name: Add dns resolver
      tags: node_dns_etc_hosts
      script: pigsty_resolv.sh

...
