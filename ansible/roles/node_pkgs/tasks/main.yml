---
- name: Install node packages
  tags: node_pkgs_install
  yum: name={{ node_package_list }}

- name: Install node build packages
  when: build_essential
  tags: node_build_pkgs_install
  yum: name={{ build_package_list }}

- name: Install node cloud native packages
  when: cloud_native
  tags: node_cloud_pkgs_install
  yum: name={{ cloud_package_list }}
...


